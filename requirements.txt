# Core dependencies for production
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
tensorflow>=2.15.0
xgboost>=2.0.0
joblib>=1.3.0

# Visualization (for notebooks and analysis)
matplotlib>=3.7.0
seaborn>=0.12.0

# Development and testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Optional: only if you need XGBoost or SARIMA (not used in production ensemble)
# statsmodels>=0.14.0
fastapi==0.136.1
uvicorn[standard]==0.30.6
pydantic==2.9.2


torch>=2.13.0
torchvision==0.28.0

# Detectron2 — must be installed from source after torch is available:
#   python -m pip install --no-build-isolation \
#     'git+https://github.com/facebookresearch/detectron2.git'

opencv-python-headless
lightgbm
timm
albumentations
boto3>=1.36.17
python-dotenv>=1.0.1

# Added for ml10
# Object detection
ultralytics>=8.0.0
# Procesamiento de imágenes
Pillow>=11.0.0

# Added for ml5 (cow behaviour) — SlowFast video classifier
# Detectron2 (detector) must be installed from source like the note above.
pytorchvideo>=0.1.5
fvcore>=0.1.5

# MLflow integration
mlflow>=2.20.0

# Pinned below mlflow's own cap (cryptography<50,>=43.0.0, as of mlflow 3.15.2).
# pip-audit flags PYSEC-2026-3552 (fixed in 50.0.0) — cannot be resolved until
# mlflow raises its upper bound; do not bump without also bumping mlflow's pin.
cryptography<50,>=43.0.0

# Added for ml34 (dairy pasteurization energy) — single-objective GA optimizer
deap==1.4.3

# Added for ml31 (cereal residue optimizer) — Linear Programming
pulp>=2.7.0

# Added for ml33 (cereal reuse-strategy optimizer) — exact MILP assignment solver
# (scipy.optimize.milp, HiGHS backend). Was previously only a transitive dep via
# scikit-learn; pinned explicitly since it's now imported directly.
scipy>=1.11.0

# Added for ml45 (grain dryer PCC detection) — DNFLExplainer's TemporalExplainer uses SHAP
# GradientExplainer for the LSTM branch. Deliberate deviation from this repo's usual pattern
# (SHAP normally delegated to the external explainability microservice) — the PCC catalog is
# this model's own functional differentiator, ported inline per explicit product decision.
# Also required by ml43 (cereal dryer anomaly/fault detection, CU43+CU44) — same DNSL family,
# same rationale: the corrective-action catalog is ported inline (see inbox/a43/manifest.yaml
# known_issues).
shap>=0.51.0