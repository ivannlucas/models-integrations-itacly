"""Vendored numerical core of the a09 delivery (feature engineering, windowing, GRU/LSTM).

These modules are near-verbatim copies of the AI team's `src/` package. They are vendored rather
than reimplemented so that predictions are bit-for-bit identical to the delivered pipeline. Every
deliberate deviation is listed in each module's docstring.
"""
